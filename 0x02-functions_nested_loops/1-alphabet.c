#include <stdio.h>
#include "main.h"
/**
 * main  a function that prints the alphabet
 * Return: Always (Success)
 */
int main(void)
{
	char low;
	for (low = 'a'; low <= 'z'; low++)
	putchar(low);
	return (0);
}
